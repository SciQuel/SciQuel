"use client";

// will delete this file later
// just wanted to view the full story data real quick
import { data } from "autoprefixer";

export default function TestStory(props: object) {
  console.log(props.storyInfo);
  return <>test stuff</>;
}
